# ====================================
# CONFIGURACIÓN DEL SERVICIO DE TRANSCRIPCIÓN
# ====================================

# Modo de ejecución:
# - full: Transcribe y formatea automáticamente
# - transcribe-only: Solo transcribe el audio
# - format-only: Solo formatea transcripciones existentes
MODE=full

# ====================================
# CONFIGURACIÓN DE WHISPER
# ====================================

# Modelo de Whisper a usar (afecta precisión y velocidad):
# - tiny: Más rápido, menos preciso (~1GB RAM)
# - base: Rápido, precisión aceptable (~1GB RAM)
# - small: Balance entre velocidad y precisión (~2GB RAM)
# - medium: Buena precisión, más lento (~5GB RAM) [RECOMENDADO]
# - large: Máxima precisión, muy lento (~10GB RAM)
WHISPER_MODEL=medium

# Idioma del audio (código ISO 639-1):
# es = Español, en = Inglés, fr = Francés, etc.
AUDIO_LANGUAGE=es

# ====================================
# CONFIGURACIÓN DE GPU (NVIDIA)
# ====================================

# Usar GPU si está disponible:
# - auto: Detecta y usa GPU automáticamente (recomendado)
# - false: Forzar uso de CPU solamente
# - true: Requiere GPU (falla si no está disponible)
USE_GPU=auto

# Límite de VRAM de GPU en MB:
# - 2048 = 2GB (recomendado para modelo medium)
# - 4096 = 4GB (para modelo large)
# - 1024 = 1GB (para modelos tiny/base/small)
# El sistema usará máximo esta cantidad de VRAM
GPU_MEMORY_LIMIT=2048

# ====================================
# CONFIGURACIÓN DE FORMATEO
# ====================================

# Motor de formateo a usar:
# - ollama: Usa modelo local (100% gratuito, sin internet) [RECOMENDADO]
# - gemini: Usa API de Google Gemini (requiere clave API y conexión)
FORMATTER=ollama

# ====================================
# CONFIGURACIÓN DE OLLAMA (Formateo Local)
# ====================================

# Modelo de Ollama a usar para formateo:
# - llama3.2:3b: Ligero y rápido (~2GB) [RECOMENDADO]
# - llama3.2:1b: Muy ligero (~1GB) 
# - llama3:8b: Más potente (~4.5GB)
# - mistral: Alternativa eficiente (~4GB)
OLLAMA_MODEL=llama3.2:3b

# ====================================
# CONFIGURACIÓN DE GOOGLE GEMINI (Opcional)
# ====================================

# API Key de Google Gemini para formateo de texto
# Obtén tu clave en: https://makersuite.google.com/app/apikey
# SOLO necesario si FORMATTER=gemini
GOOGLE_API_KEY=

# Modelo de Gemini a usar (solo si FORMATTER=gemini):
# - gemini-1.5-flash-latest: Más rápido y económico
# - gemini-1.5-pro-latest: Más calidad y contexto
GEMINI_MODEL=gemini-1.5-pro-latest
